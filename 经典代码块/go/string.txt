if anthVar == "" || !strings.HasPrefix(anthVar, "token") {
    c.Set(ContextUserError, errors.New("user not find"))
}

strings.Replace(anthVar, "token ", "", 1)
    fmt.Println(strings.Replace("oink oink oink", "k", "ky", 2))
    fmt.Println(strings.Replace("oink oink oink", "oink", "moo", -1))
    oinky oinky oink
    moo moo moo