if anthVar == "" || !strings.HasPrefix(anthVar, "token") { // 字符串以token开头
   
}

strings.Replace(anthVar, "token ", "", 1)
    fmt.Println(strings.Replace("oink oink oink", "k", "ky", 2))  // 替换两次 oinky oinky oink
    fmt.Println(strings.Replace("oink oink oink", "oink", "moo", -1)) // 全部替换 moo moo moo
    
    