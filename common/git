ssh-keygen -t rsa -C "735761545@qq.com" // 三次回车即生成ssh-key,然后配置好
	git init
	git status
	git add .
	git status
	git commit -am 'first commit into project'
	git remote add origin git@gitee.com:XiuMianTi/maven_learning.git
	git pull
	git push -u origin master
	git push -u origin master -f
	git branch
	git branch -r
	git checkout -b v1.0 origin/master
	git branch
	git push origin HEAD -u
	history